#include <stdio.h>
#include <string.h>

// 8 algos
// matrix dinamica
// 1000 linhas
// short dentro da matriz

int main()
{
    int i = 0, j = 0;
    char v[1000][1000];
    short *run;

    while (scanf("%s", v[i][j]) != EOF)
    {
        if (v[i][j] == '\0')
        {
            j++;
        }
    }

    return 0;
}